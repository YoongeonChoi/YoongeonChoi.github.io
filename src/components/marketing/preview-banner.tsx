export function PreviewBanner() {
  return (
    <div className="border-b-2 border-border bg-accent px-4 py-3 text-sm font-medium text-accent-foreground">
      Preview mode is active. This response is intentionally `noindex` and `no-store`.
    </div>
  );
}
