export default function AdminContactPage() {
  return (
    <section className="section-frame p-8">
      <p className="eyebrow">Admin / Contact</p>
      <h1 className="mt-6 font-heading text-4xl font-bold tracking-tight">Inbox triage</h1>
      <p className="prose-width mt-6 text-text-muted">
        Inquiry intake will be stored with minimum data collection, Turnstile verification, encrypted
        fields, and explicit close / retention workflows.
      </p>
    </section>
  );
}
